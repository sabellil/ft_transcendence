services:
  postgres:
    image: postgres:16
    container_name: transcendence_postgres

    env_file:
      - .env
#rediriger le port 5432 de notre machine sur le conteneur PostgreSQL qui ecoute sur son port 5432 
    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: